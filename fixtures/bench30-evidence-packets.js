// Intentionally empty. No locally retained T0 evidence packet existed at plan
// start. Keeping the hold explicit prevents a 30-slot schema from being called a
// completed corpus.
export const BENCH30_EVIDENCE_PACKETS = Object.freeze([]);
