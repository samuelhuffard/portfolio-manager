Each subdirectory is one independent research agent: own `watchlist.json`, `weights.json`, `risk-limits.json`, and `personality.md`.

`personality.md` is read verbatim and appended to the AI overlay's system context. Agent 1, Agent 2, and Agent 3 each have a compact runtime mandate; their fuller versioned specifications sit beside them as `AGENT-*-PLAN.md`. Keep both forms aligned with their canonical mandate sources. A compact prompt is not permission to bypass deterministic gates, ownership, evaluator review, or human approval.
